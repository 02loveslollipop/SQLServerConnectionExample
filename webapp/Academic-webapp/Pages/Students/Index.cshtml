@page
@model Academic_webapp.Pages.Students.IndexModel
@{
    ViewData["Title"] = "Students";
}

<h1 class="display-6">Students</h1>

@if (!string.IsNullOrWhiteSpace(Model.StatusMessage))
{
    <div class="alert alert-success">@Model.StatusMessage</div>
}

<p>
    <a class="btn btn-primary" asp-page="Create">Add Student</a>
</p>

<table class="table table-striped table-bordered align-middle">
    <thead class="table-light">
        <tr>
            <th>Student</th>
            <th>Major</th>
            <th>Department</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Students)
{
        <tr>
            <td>@item.FullName</td>
            <td>@item.Major?.MajorName</td>
            <td>@item.Major?.Department?.DepartmentName</td>
            <td class="text-end">
                <a class="btn btn-outline-secondary btn-sm" asp-page="Details" asp-route-id="@item.StudentID">Details</a>
                <a class="btn btn-outline-primary btn-sm" asp-page="Edit" asp-route-id="@item.StudentID">Edit</a>
                <a class="btn btn-outline-danger btn-sm" asp-page="Delete" asp-route-id="@item.StudentID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
