@page
@model ErrorModel
@{
    ViewData["Title"] = "Error";
}

<h1 class="text-danger">Error.</h1>
<h2 class="text-danger">Ocurrio un error al procesar la solicitud.</h2>

@if (Model.ShowRequestId)
{
    <p>
    <strong>Id de solicitud:</strong> <code>@Model.RequestId</code>
    </p>
}

<h3>Modo de desarrollo</h3>
<p>
    Cambiar al entorno de <strong>Development</strong> muestra informacion detallada acerca del error ocurrido.
</p>
<p>
    <strong>El entorno Development no debe estar habilitado en aplicaciones en produccion.</strong>
    Podria exponer informacion sensible de las excepciones a los usuarios finales.
    Para depuracion local habilita el entorno <strong>Development</strong> estableciendo la variable <strong>ASPNETCORE_ENVIRONMENT</strong> en <strong>Development</strong>
    y reiniciando la aplicacion.
</p>
